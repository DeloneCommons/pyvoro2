# v0.9.0 development plan — functional and API stabilization

- **Status:** Active
- **Plan revision:** 0.4
- **Target release:** v0.9.0
- **Base release:** v0.8.0
- **Planning baseline source:** `db0884c641de0998d190de8aeee1d45154e46aff`
- **Final independent review source:** `39d2223efb364a164db4e5d82125d2c2986494e8`
- **Integration branch:** `dev`
- **Maintainer:** Ivan Yu. Chernyshov
- **Approved:** 2026-09-01 — Ivan Yu. Chernyshov
- **Last revised:** 2026-09-01
- **GitHub milestone:** [`v0.9.0`](https://github.com/DeloneCommons/pyvoro2/milestone/3)
- **Activation issue:** [#46](https://github.com/DeloneCommons/pyvoro2/issues/46)
- **Execution tracker:** [#47](https://github.com/DeloneCommons/pyvoro2/issues/47)
- **Primary decision:** [ADR 0017](../decisions/0017-v0.9-functional-stabilization-before-1.0.md)

This active plan transfers the maintainer-approved v0.9 planning decisions from
the code-first architectural review into repository authority and incorporates
the final independent pre-implementation review against current HEAD. Issue #46
records activation and WP0 contract work; issue #47 tracks substantive WP1–WP13
execution. Substantive child issues are prepared just-in-time from the then-current
`dev` state. Activation authorizes only the work and decisions recorded here; it
is not blanket approval to expand v0.9 scope.

## Intended outcome

v0.9.0 should be the first release whose existing forward, periodic, and
separator-inverse core can seriously be treated as a candidate for the public
semantics of 1.0.

The release should leave pyvoro2 with:

- coherent mathematical-weight and explicit-radius input semantics across
  `compute()`, `locate()`, and `ghost_cells()`;
- periodic user-lattice semantics that are distinct from the Voro++ backend
  representation and that accept valid left- and right-handed bases without
  reordering user lattice vectors;
- representation-robust certified nearest/minimum-image and duplicate-safety
  geometry for supported periodic domains, including deliberately poor but
  equivalent lattice representations within explicit structural resource
  limits;
- certified periodic face/edge and ghost image metadata whose correctness no
  longer depends on a user-selected finite search window;
- all public periodic integer shifts interpretable in the user-supplied lattice
  basis with one documented sign convention;
- a normal supported realization-aware separator workflow whose stable result
  contract remains distinct from the empirical convergence properties of its
  outer active-set refinement;
- separator mismatch, hard-constraint, and soft-penalty measurement spaces that
  can be selected independently without introducing the future generic mixed
  observation-block architecture;
- public-workflow qualification that uses only supported APIs and tests
  user-visible invariants rather than implementation details;
- a final pre-1.0 API/lifecycle audit that removes known compatibility artifacts
  rather than carrying them into the v0.9.x downstream soak period.

The released v0.9.x series is then the downstream-readiness/soak phase. v0.9.0
must remove known critical semantic and API holes, but it is not required to
prove that real downstream use can never discover another issue. v1.0 is gated
on that soak showing that the existing forward, periodic, and separator-inverse
core has no remaining critical correctness or API gap requiring another known
incompatible redesign.

## Baseline and motivation

The reviewed v0.8.0 source already contains substantial stabilization work that
should be reused rather than redesigned:

- `compute(..., mode="power")` already accepts mathematical `weights=` and
  converts them to the Voro++ radius representation with one common gauge;
- ADR 0012 and `pyvoro2._internal.periodic_images` already provide certified
  nearest/minimum-image geometry whose successful result does not depend on a
  finite user search cube;
- ADR 0013 centralizes generator preparation and mandatory backend-safety
  duplicate checks;
- ADRs 0014 and 0015 already define separator observation/source identity and
  atomic realization-aware final-state semantics;
- the active-set implementation already keeps outer termination separate from
  the final inner-fit status and represents cycles, iteration limits,
  infeasibility, and numerical failure structurally;
- the existing examples and integration tests already provide a qualification
  scaffold that can be extended instead of replaced.

The remaining pre-1.0 problems are concentrated rather than package-wide.

`locate()` and `ghost_cells()` still lack weight-first parity. `PeriodicCell`
currently conflates mathematical validity with orientation/conditioning choices
that belong to backend or proof machinery. Public user-cell fractional geometry
is not separated cleanly from Voro++-primary remapping. Certified nearest-image
search can become impractical for a very poor equivalent lattice basis, and the
same representation sensitivity can reach mandatory periodic duplicate safety.
Periodic face/edge image reconstruction still depends on finite search-window
machinery. Periodic ghost cells expose an additional correctness problem:
native neighbor integers do not reliably distinguish persistent generators,
periodic images of the temporary ghost itself, and walls.

The separator inverse architecture is already much closer to the intended 1.0
shape. The main remaining semantic issue is that the observation measurement
space currently also controls mismatch, hard bounds, and scalar penalties even
though these are independent scientific choices. The ordinary
realization-aware workflow also still reaches an advanced/experimental-level
surface even though its result and termination model is already largely
stabilized.

This plan therefore prioritizes coherent periodic foundations first, then
periodic boundary/metadata semantics, then the smaller inverse stabilization
workstream, followed by public-workflow qualification and a final API audit.

## Accepted constraints

The following existing decisions and planning constraints remain authoritative
unless an issue explicitly updates the relevant decision record:

- [ADR 0002](../decisions/0002-weights-radii-and-gauge.md): mathematical
  weights and backend radii are distinct representations; one global additive
  weight shift is geometric gauge;
- [ADR 0012](../decisions/0012-certified-periodic-image-geometry.md): nearest
  image inference must be certified and `image_search` is only a
  correctness-neutral incumbent/performance hint; its v0.9 amendment uses the
  D1 physical-space exact-tie rule;
- [ADR 0013](../decisions/0013-central-generator-preparation-and-backend-safety.md):
  generator preparation and mandatory duplicate safety stay centralized;
- [ADR 0014](../decisions/0014-separator-observation-and-source-identity.md):
  observation/source identity remains separate from model/solver choices;
- [ADR 0015](../decisions/0015-atomic-separator-active-state.md): realization-aware
  accepted state is atomic and outer termination remains separate from the
  final inner-fit status;
- [ADR 0016](../decisions/0016-severity-complete-tessellation-diagnostics.md):
  diagnostic severity semantics remain explicit and complete;
- [ADR 0017](../decisions/0017-v0.9-functional-stabilization-before-1.0.md): v0.9
  is the functional/API stabilization release before a v0.9.x downstream soak
  and the stable 1.0 core;
- [ADR 0018](../decisions/0018-periodic-user-lattice-and-boundary-semantics.md):
  user lattice, backend frame, proof basis, public shifts, and certified ghost
  boundary identity are separate periodic contracts;
- [ADR 0019](../decisions/0019-separator-measurement-spaces-and-supported-realization.md):
  separator observation/model spaces are independent and ordinary
  realization-aware fitting has a distinct supported facade.

Additional accepted constraints for this plan are:

- prefer adapting the current abstractions to creating new public frameworks;
- do not preserve an awkward pre-1.0 public API solely for hypothetical
  compatibility;
- keep one repository and one distribution containing the forward/native
  wrapper and inverse functionality through 1.0;
- reassess package/backend separation only after 1.0 and only under a concrete
  trigger, especially a credible alternative Voronoi backend with an existing
  Python interface that makes backend-neutral inverse logic practically
  valuable;
- do not run Voro++ automatically on a privately reduced equivalent basis in
  v0.9 unless a later issue establishes a compelling reason and a new
  maintainer decision;
- do not add a public Niggli/Minkowski/reduction API merely because private
  reduction infrastructure exists;
- do not redesign the documentation stack during v0.9.0 functional
  implementation; only factual/API/capability maintenance required by accepted
  code changes is a v0.9.0 release gate; content/information-architecture and
  visual/engine work require separate v0.9.x soak-period plans;
- the known extreme power-weight/Voro++ dynamic-range limitation is not a v0.9
  feature requirement and must not be replaced by an unjustified universal
  numerical cutoff.

## Accepted maintainer decisions

The following eight decisions are settled for this plan. WP0 records durable
parts in ADRs and the target API inventory before production implementation
relies on them.

### D1 — Exact periodic ties are selected in physical space

When several periodic images are exactly tied, candidate selection uses the
exact Cartesian displacement rather than integer coefficients in the supplied
lattice basis. In fixed caller Cartesian axes, orientation `+1` selects the
lexicographically smallest exact displacement tuple and orientation `-1` the
largest. The selected physical image is then expressed in the user's basis.

This total order is exact, invariant under an exact unimodular change of lattice
basis, antisymmetric under pair reversal, and covariant when either endpoint is
translated by a lattice vector. It deliberately changes the ADR 0012
coefficient-space rule. The contract does not claim invariance under an
arbitrary global rotation: choosing one deterministic member of a rotationally
symmetric exact tie necessarily uses a reference convention.

### D2 — `PeriodicCell` mathematical validity is separate from representation quality

A periodic lattice is mathematically valid when its finite binary64 basis is
non-degenerate; a negative determinant denotes a left-handed basis rather than
an invalid one. Poor conditioning can produce a warning, an operation-level
backend-representability failure, or a certified-geometry resource failure, but
must not by itself be conflated with mathematical invalidity.

Because this admits poorly conditioned but exact non-degenerate bases, the
integer shift for deterministic half-open user wrapping must be decided from
the exact dyadic basis, origin, and input point. A rounded inverse followed by
`floor`, epsilon snapping, or nearest-integer repair is not the authority for
the half-open cell decision. Returned binary64 coordinates are validated rounded
views of that exact decision. WP2 must define the visible boundary convention for
an exact remainder that rounds to an upper endpoint: rounding must not silently
change the exact wrap shift, and the float view must satisfy a documented
reconstruction envelope rather than an epsilon-selected cell decision.

### D3 — Ghost boundary identity uses an explicit data contract

Periodic ghost boundaries must distinguish conceptually among:

- a persistent generator image;
- another periodic image of the temporary ghost itself;
- a non-periodic wall.

Native positive/negative integer neighbor values are not the semantic authority
for this distinction. The internal/public contract is a tagged record whose
kind and payload fields are validated together. A representative conceptual
shape is:

```text
BoundaryReference
    kind: generator | ghost_self | wall
    generator_id: int | None
    shift: integer tuple | None
    wall_id: int | None
```

The exact public spelling is fixed during WP0/API-inventory work rather than by
this example. The semantic invariants are:

| Kind | Generator ID | User-basis shift | Wall ID |
|---|---|---|---|
| persistent generator | required | required where periodic image identity applies | none |
| ghost self-image | none | required and non-zero | none |
| wall | none | none | required where the existing domain exposes wall identity |

Existing ordinary `compute()` adjacency fields need not be broken merely for
schema symmetry, but internal consumers must stop treating integer sign alone
as boundary kind. The ghost API must not expose an undefined native integer as
an owner identifier.

A tagged semantic boundary record is assigned only to a boundary certified to
have positive measure. A raw native face/edge certified to have zero measure is
a non-semantic numerical artifact; a record whose measure cannot be resolved
within the declared envelope produces structured ambiguity when certified
metadata is requested. The API inventory must state whether such raw records are
retained, normalized away, or exposed separately.

Candidate assignments may be deduplicated as physically equivalent only when
they define the same exact cut and the same public semantic provenance
(`kind`, owner, and user-basis shift). Different owners, shifts, or boundary
kinds are not made equivalent merely by similar residuals; they require trusted
disambiguating evidence or a structured ambiguity/multiplicity result.

### D4 — Obsolete face/edge reconstruction controls are removed immediately

After certified boundary-image reconstruction lands, correctness-critical
public knobs that no longer have a scientific meaning are removed rather than
carried through a deprecation cycle. This includes the applicable
face/edge-shift search, validation, repair, and reconstruction-matching tolerance
arguments.

Separator `image_search` is different and remains a correctness-neutral
performance seed under ADR 0012. General tessellation diagnostic tolerances are
also separate and are not removed merely because they are tolerances.

### D5 — Ghost power representation gets the final names immediately

`ghost_cells()` supports two complete power-representation families:

```python
weights=..., ghost_weights=...
```

or:

```python
radii=..., ghost_radii=...
```

Mixed weight/radius families are rejected. The old `ghost_radius` spelling is
removed directly; no deprecated alias is carried through v0.9.

For mathematical weights, persistent generator weights and all temporary ghost
weights in one call share one common weight-to-radius gauge conversion.

### D6 — Separator measurement axes are independent

The space in which an observation target was supplied is distinct from the
space used by:

- mismatch/objective evaluation;
- hard feasibility constraints;
- each scalar soft penalty.

`SeparatorObservations.measurement` continues to describe the observation
source/target space. Model terms gain their own optional space, and an omitted
term space inherits the observation space so existing same-space models retain
their meaning.

This does not introduce arbitrary per-row spaces, strengths, robust-loss scales,
site anchors, or a generic mixed observation-block protocol.

### D7 — Realization-aware fitting has a distinct supported high-level function

The fixed-observation separator solve and empirical realization-aware outer
refinement remain visibly different algorithms. Ordinary realization-aware use
moves under `pyvoro2.inverse` through a separate supported high-level function
rather than a mode flag on `fit_weights_from_separators()`.

The current active engine and `SelfConsistentPowerFitResult` should be reused.
Advanced path/hysteresis controls can remain in the advanced separator
namespace without becoming 1.0 commitments.

### D8 — Public point-centered/symmetric-bound convenience is not a v0.9 gate

The measurement-space redesign should make point-centered or symmetric bounds
straightforward internally, but v0.9 does not add a public convenience type or
syntax unless implementation exposes a concrete need that receives a separate
maintainer decision.

## Remaining decision gate from the existing roadmap

ADR 0017 and the roadmap retain one broader backend-policy decision that is not
settled by D1–D8:

| ID | Question | Why it matters | Resolution | Status |
|---|---|---|---|---|
| D9 | Should pyvoro2 formally adopt a no-persistent-pyvoro2-specific functional Voro++ fork policy, including how narrow correctness/upstream-backport patches are handled? | Current HEAD proves that WP7 requires a narrow 3D native-bridge correction; release policy must distinguish that correction from maintaining a divergent functional fork. | Maintainer decision/ADR no later than WP13. If WP7 needs a vendored Voro++ source patch, settle D9 before accepting that patch; a pybind-only correction does not settle the policy. | Open |

D9 does not block Python work or a narrow pybind correction. The current 3D
ghost routes nevertheless require a native-bridge fix that prevents an
uninitialized temporary ghost ID from being read. If satisfying that requirement
needs a vendored Voro++ source patch rather than a binding-only path, work stops
for the D9 maintainer decision before the patch is accepted. A substantive
persistent backend behavior change must never settle D9 implicitly.

## Core periodic architecture

The periodic workstream uses three conceptually separate layers. The
implementation may share storage or helpers where appropriate, but public and
proof semantics must not collapse these layers again.

### User lattice layer

Let the user-supplied lattice vectors be rows of `A` and let `o` be the origin.
Public user-cell geometry follows

```text
x = o + f @ A
```

where `f` is a fractional row vector.

This layer owns:

- Cartesian-to-fractional conversion;
- fractional-to-Cartesian conversion;
- fractional wrapping modulo the integer lattice;
- Cartesian wrapping into the user parallelepiped;
- user-basis integer shifts;
- public reconstruction equations and shift signs;
- the handedness of the user-supplied basis.

All public periodic integer shifts must ultimately be interpretable in this
basis.

### Backend periodic-frame layer

Voro++ uses a lower-triangular periodic representation. The preferred design to
verify and implement is a centralized sign-normalized orthogonal factorization,
for example through QR of the transpose,

```text
A.T = Q @ R
L = R.T = A @ Q
x_backend = (x - o) @ Q
x = o + x_backend @ Q.T
```

with coordinated sign normalization of columns of `Q` and rows of `R` so the
lower-triangular backend matrix `L` has positive diagonal entries.

For a right-handed user basis, `Q` is a proper rotation; for a left-handed
basis it may be an improper orthogonal transformation/reflection. This changes
the internal frame without permuting user lattice vectors or changing integer
coefficient semantics.

This layer owns:

- Cartesian/internal transformations;
- Voro++ lower-triangular parameters;
- Voro++ primary-domain remapping;
- backend-used generator/query coordinates;
- transformation of native geometry back to Cartesian coordinates.

Existing `remap_cart()` and `remap_internal()` retain their Voro++-primary
meaning. New user-parallelepiped operations must receive distinct names.

An improper transformation can reverse orientation. WP2 must therefore audit
ordered face-vertex cycles, normals, and any signed-orientation assumptions
rather than assuming that distances and unsigned measures are the only affected
quantities.

### Periodic coordinate vocabulary

Periodic query and generator paths distinguish four concepts even when an
implementation can share storage:

- original input Cartesian coordinates;
- user-parallelepiped wrapped coordinates plus the exact user-basis wrap shift;
- backend-primary/cell-site Cartesian coordinates plus any backend remap shift;
- native internal-frame coordinates.

`p_ghost_site` means the active cell-site representative that anchors returned
ghost geometry. It is never the original unwrapped query. If implementation
reanchors geometry before packaging, every associated image shift must be
transformed consistently and the result must still satisfy its documented
reconstruction equation.

### Exact lattice-arithmetic layer

Finite binary64 lattice components, origins, and exact user inputs are
interpreted as dyadic rationals where they control a discrete decision. This
layer owns:

- exact determinant/non-singularity and determinant sign;
- exact fractional solve/floor decisions for user half-open wrapping;
- exact user-basis wrapping shifts and their reconstruction checks;
- certified unique integer-translation recovery for approximate native
  Cartesian image positions within a declared numerical envelope;
- exact proof data used by certified periodic geometry;
- private exact unimodular basis reduction;
- exact mappings between reduced-basis and user-basis shifts;
- proof-complete integer candidate enumeration;
- representation-invariant exact tie comparison;
- structural work/resource accounting.

For a private reduced row basis

```text
A_reduced = U @ A
```

where `U` is an exact integer unimodular matrix, shift mappings are

```text
s_user = s_reduced @ U
s_reduced = s_user @ U_inverse
```

using exact integer arithmetic. Python integers are used internally; the
existing public signed-int64 shift range is checked only when a public shift is
materialized.

The exact determinant, fractional solve/floor, and integer-translation
primitives are foundational and may be introduced in WP2. WP3 extends the same
private layer with unimodular reduction; it does not own the first exact
operation merely because reduction is its main feature.

The private reducer is selected for the computational goal of certified
closest-vector/minimum-image and related lattice enumeration. Crystallographic
canonicalization is not the goal, so Niggli reduction is not assumed to be the
right strategy.

### Boundary-certification numerical authority

Certification describes compatibility with the approximate binary64 geometry
actually returned by the native backend, within an explicit internal envelope.
It does not claim access to an unobserved exact Voro++ face or edge.

For power diagrams, original mathematical weights remain the public scientific
representation, but candidate-plane compatibility uses the exact binary64 radii
actually supplied to Voro++. The envelope covers the backend radius-squaring and
plane-offset arithmetic. Implementation need not emulate compiler instruction
order bit-for-bit, but it must bound the resulting discrepancy.

A recovered owner/image translation from a native Cartesian position is accepted
only when exactly one integer shift is compatible within that envelope. An exact
inverse of the approximate native bit pattern alone is not a proof of the
intended translation. Ambiguity is a structured failure, never a nearest-residual
guess.

### Qualified representation-robustness claim

v0.9 does not automatically run Voro++ on the privately reduced equivalent
basis. Consequently the release makes two deliberately different claims:

1. pyvoro2's certified lattice geometry should remain practical for
   substantially poorer equivalent representations, subject only to explicit
   private structural resource limits;
2. end-to-end tessellation is qualified on deliberately poor but still
   backend-resolvable equivalent bases, not on arbitrary extreme unimodular
   shears that Voro++ itself may fail to represent or resolve reliably.

A mathematically valid cell can therefore construct successfully and later
produce a clear backend-representability or certified-resource failure. This is
preferable to calling the lattice itself invalid.

## Scope

The required release work is represented by WP0–WP14 below. The packages are
coherent acceptance units, not function-by-function tasks. WP0 is tracked by
issue #46. Substantive WP1–WP13 execution is tracked by #47, with focused child
issues prepared just-in-time from the current `dev` state rather than created in
advance. WP14 remains a later frozen-source qualification issue after WP13.

| ID | Work package | Required for v0.9.0? | Depends on | Issue/tracker |
|---|---|---|---|---|
| WP0 | Activate v0.9 contracts and release plan | Yes | — | [#46](https://github.com/DeloneCommons/pyvoro2/issues/46) |
| WP1 | Weight-first `locate()`/`ghost_cells()` parity | Yes | WP0 | [#47](https://github.com/DeloneCommons/pyvoro2/issues/47); child issue JIT |
| WP2 | User lattice semantics and orientation-neutral backend frame | Yes | WP0 | [#47](https://github.com/DeloneCommons/pyvoro2/issues/47); child issue JIT |
| WP3 | Exact private unimodular lattice reduction | Yes | WP0, WP2 shared exact primitives | [#47](https://github.com/DeloneCommons/pyvoro2/issues/47); child issue JIT |
| WP4 | Representation-robust minimum images, duplicate safety, and certified native translation recovery | Yes | WP2, WP3 | [#47](https://github.com/DeloneCommons/pyvoro2/issues/47); child issue JIT |
| WP5 | Certified 3D persistent face-image shifts | Yes | WP2–WP4 | [#47](https://github.com/DeloneCommons/pyvoro2/issues/47); child issue JIT |
| WP6 | Certified 2D edge owner/image reconstruction | Yes | WP2–WP4, shared certification contract; normally after WP5 | [#47](https://github.com/DeloneCommons/pyvoro2/issues/47); child issue JIT |
| WP7 | Periodic ghost identity and certified ghost shifts | Yes | WP1, WP5, WP6 | [#47](https://github.com/DeloneCommons/pyvoro2/issues/47); child issue JIT |
| WP8 | Coherent periodic query/owner/ghost metadata and internal/public geometry separation | Yes | WP2, WP4, WP7 | [#47](https://github.com/DeloneCommons/pyvoro2/issues/47); child issue JIT |
| WP9 | Periodic search/tolerance/repair lifecycle cleanup | Yes | WP5–WP8 | [#47](https://github.com/DeloneCommons/pyvoro2/issues/47); child issue JIT |
| WP10 | Independent separator measurement spaces | Yes | WP0; periodic-independent | [#47](https://github.com/DeloneCommons/pyvoro2/issues/47); child issue JIT |
| WP11 | Supported realization-aware separator facade | Yes | WP10 | [#47](https://github.com/DeloneCommons/pyvoro2/issues/47); child issue JIT |
| WP12 | Public workflow qualification | Yes | WP1–WP11 | [#47](https://github.com/DeloneCommons/pyvoro2/issues/47); child issue JIT |
| WP13 | Final pre-1.0 API/lifecycle audit | Yes | WP12 | [#47](https://github.com/DeloneCommons/pyvoro2/issues/47); child issue JIT |
| WP14 | Frozen-source v0.9.0 release qualification | Yes | WP13 | Later release-qualification issue |

### WP0 — Activate the release plan and semantic contracts

**Purpose**

Transfer the durable v0.9 decisions into repository authority before production
implementation relies on them.

**Deliverables**

- record this plan as Active after maintainer approval and `v0.9.0` milestone linkage;
- amend or supersede the exact-tie portion of ADR 0012;
- add a periodic semantic ADR covering the three-layer lattice model,
  left-handed input, exact half-open wrapping authority, coordinate vocabulary,
  public shift equations, positive-measure ghost/boundary kinds, structural
  resource failure, certified native translation recovery, and boundary
  reconstruction semantics;
- add an inverse measurement-space ADR covering observation source space,
  independent model-term spaces, inheritance, and the v0.9 heterogeneity
  boundary;
- update the target API inventory with intended public names/removals/result
  fields before those surfaces are implemented;
- link the active plan to milestone `v0.9.0`, activation issue #46, and
  substantive execution tracker #47; prepare later child issues just-in-time.

**Acceptance criteria**

- D1–D8 are represented in repository artifacts without changing their meaning;
- periodic query/owner/boundary sign conventions are stated as reconstruction
  equations rather than prose alone;
- user lattice, backend frame, and reduced proof basis are explicitly distinct;
- ghost boundary kinds, positive-measure eligibility, and payload invariants are
  explicit;
- the exact Cartesian tie total order, exact user-wrap shift authority, and
  certified approximate-native translation rule are explicit;
- backend-effective power-plane authority and exact provenance-equivalence rules
  are explicit;
- report/schema migration strategy for mixed separator spaces is explicit;
- no unresolved policy choice blocks WP1–WP11; D9 remains a release-audit gate
  and becomes an earlier stop condition before any vendored Voro++ source patch
  needed by the mandatory WP7 native-bridge correction;
- plan status is `Active` only with recorded maintainer approval and milestone
  linkage.

WP0 is maintainer/reasoning-review work. It should not delegate new public
policy to a coding agent.

### WP1 — Add weight-first `locate()` and `ghost_cells()` workflows

**Purpose**

Bring query APIs to the mathematical-weight/explicit-radius representation
model already used by `compute()`.

**Main code areas**

- `pyvoro2._internal.power_input` and weight transforms;
- spatial and planar public API modules;
- central generator preparation where reusable;
- forward query tests.

**Required behavior**

- `locate(..., mode="power")` accepts exactly one of `weights=` or `radii=`;
- `ghost_cells(..., mode="power")` accepts either the complete pair
  `weights=`/`ghost_weights=` or the complete pair `radii=`/`ghost_radii=`;
- a mathematical-weight ghost call computes one common gauge from persistent
  and temporary ghost weights together, then splits the converted backend radii
  back into their persistent and temporary roles;
- mixed weight/radius representation families are rejected clearly;
- `ghost_radius` is removed directly, without a deprecated alias;
- standard mode rejects power-specific arguments consistently in 2D and 3D.

**Acceptance criteria**

- weight calls match equivalent explicit-radius calls;
- adding one constant to all persistent and ghost weights leaves geometry
  invariant;
- independent gauge shifts of only the persistent or only the ghost set are not
  accidentally normalized away;
- existing explicit-radius behavior remains unchanged;
- 2D/3D signatures and validation semantics are aligned where the concept is
  shared;
- no new public result hierarchy is introduced solely for input parity.

### WP2 — Separate user lattice semantics from the backend periodic frame

**Purpose**

Make `PeriodicCell` an honest user-lattice object while preserving a distinct
Voro++-oriented backend frame and accepting valid left-handed bases without
permuting user vectors.

**Main code areas**

- `domains.py`;
- detached spatial/domain geometry snapshots;
- generator preparation;
- existing visualization/user-parallelepiped wrapping helpers;
- periodic-cell and remap tests.

**Required behavior**

- exact nonzero determinant, not determinant sign or a heuristic condition
  threshold, determines mathematical non-degeneracy;
- one centralized sign-normalized RQ/QR-derived frame replaces duplicated frame
  construction;
- the backend lower-triangular diagonal remains positive;
- user vector order and integer coefficient meaning are unchanged;
- public operations exist for Cartesian/fractional conversion, fractional
  modulo wrapping, and Cartesian wrapping into the user parallelepiped;
- user wrapping follows one deterministic half-open convention; its integer
  user-basis shift is decided from exact dyadic basis/origin/input arithmetic,
  not a rounded inverse plus `floor`, epsilon snapping, or repair;
- returned binary64 wrapped/fractional coordinates are validated rounded views of
  that exact discrete decision; exact interior remainders that round to an upper
  endpoint follow a documented float-view/reconstruction-envelope rule without
  changing the exact shift;
- `remap_cart()`/`remap_internal()` retain their Voro++-primary semantics;
- conditioning/shape diagnostics are separated from mathematical validity;
- operation preparation reports backend-unrepresentable geometry at the stage
  that actually needs the backend representation.

**Acceptance criteria**

- left- and right-handed non-degenerate bases are accepted;
- user coordinate round trips and wrap reconstruction equations pass, including
  exact seams, exact interior values whose nearest binary64 view reaches an upper
  endpoint, one-ULP points on both sides, negative coordinates, large unimodular
  shears, and left-handed bases;
- existing right-handed public numerical results remain equivalent within the
  established contracts;
- ordered face winding, normals, and signed-orientation assumptions are audited
  under improper transformations;
- poor but mathematically valid bases are not rejected solely by the current
  heuristic condition/relative-volume checks;
- narrow factual documentation is corrected where current text conflates user
  and backend primary domains or states outdated pre-wrapping behavior.

### WP3 — Add exact private lattice-basis reduction and shift mappings

**Purpose**

Provide one reusable private exact reduction primitive for proof-oriented
periodic geometry rather than burying an ad hoc reducer inside one caller.

**Main code areas**

- the shared private exact lattice-arithmetic/reduction module introduced or
  contracted by WP2;
- exact dyadic helpers currently owned by periodic-image code;
- private caching/resource policy;
- adversarial representation fixtures and test-only oracles.

**Required contract**

For `A_reduced = U @ A`:

- `U` and `U_inverse` are exact integer matrices;
- `det(U) = +/-1`;
- the equality and inverse relationship are verified exactly;
- reduced/user integer shifts round-trip exactly;
- no public reduction API is exported;
- Voro++ continues to use the normal backend frame, not the reduced proof basis.

**Algorithm-selection requirement**

The issue must compare deterministic exact rank-3 reduction strategies against
the computational objective that matters here:

- proof-derived coefficient-box/candidate count;
- maximum coefficient growth;
- exact integer bit growth;
- deterministic termination;
- behavior under adversarial unimodular shears.

A crystallographic canonicalizer such as Niggli reduction is not selected merely
because it is standard. Reasonable candidates include exact LLL-like shortening
and fixed-rank Minkowski/Selling-like strengthening or a deterministic
combination, but the final private method is chosen from evidence rather than
this plan naming a public algorithm.

**Acceptance criteria**

- WP2 exact determinant/wrap primitives are reused and extended rather than
  duplicated;
- finite binary64 basis components are represented exactly as aligned dyadics;
- exact transform/mapping invariants pass across deterministic and randomized
  unimodular fixtures;
- structural resource limits are explicit private limits on proof work and
  never authorize an incomplete/heuristic answer;
- exceeding such a limit yields a structured resource failure;
- the chosen reducer materially reduces proof workload on the repository's poor
  representation fixtures and additional large-shear cases;
- a short correctness note and an independent bounded brute-force/test oracle
  exist before the production implementation is accepted.

**Stop condition**

If no evaluated exact strategy gives useful workload reduction with tractable
mapping/termination semantics, stop for architecture review. Do not replace the
requirement with a hidden finite search radius.

### WP4 — Use reduced bases for certified periodic geometry and native translation recovery

**Purpose**

Make current certified nearest/minimum-image consumers practical under poor
but equivalent lattice representations while preserving rigorous
proof-complete semantics, and provide the reusable certification primitive for
recovering integer lattice translations from approximate native Cartesian image
positions.

**Main code areas**

- `pyvoro2._internal.periodic_images`;
- periodic duplicate scanning and exact triclinic candidate/bucket layout;
- separator nearest-image inference;
- certified integer-translation recovery for approximate native image positions;
- representation-invariance tests.

**Required behavior**

- proof-complete triclinic enumeration uses the private reduced basis;
- the selected shift is mapped exactly back to the user basis before public
  materialization;
- public signed-int64 range checking occurs after mapping;
- `image_search` remains a correctness-neutral incumbent seed;
- exact ties use D1's physical-displacement rule;
- mandatory duplicate-safety candidate generation uses the reduction
  infrastructure wherever poor representation would otherwise make the safety
  proof impractical;
- approximate native Cartesian image positions yield an integer lattice
  translation only when exactly one candidate is compatible within the declared
  native numerical envelope; otherwise recovery fails structurally.

For an exact unimodular basis change `A_prime = U @ A`, corresponding shifts
must transform covariantly. Test fixtures must establish this equality in exact
dyadic arithmetic; a rounded matrix product that changes the represented lattice
is not an invariance oracle.


```text
s_user = s_prime @ U
```

while the physical displacement and distance remain unchanged.

**Acceptance criteria**

- changing `image_search` cannot change a successful certified result;
- the known poor-basis structural-work regression succeeds within the accepted
  private workload budget or fails only by an explicit structural limit;
- bounded random determinant `+1` and `-1` unimodular transformations preserve
  physical minimum-image geometry;
- pair reversal, lattice-translation covariance, and D1's exact Cartesian total
  tie order pass;
- native translation fixtures cover unique, ambiguous, and inconsistent
  compatibility cases;
- mandatory periodic duplicate classification is representation-robust across
  the same qualified fixture family;
- current orthorhombic/partially periodic fast paths remain available;
- no approximate fallback is returned after a proof/resource failure.

### Integration checkpoint A — Periodic foundations

After WP1–WP4, perform an explicit adversarial acceptance pass before boundary
metadata work is allowed to redefine any foundation.

The checkpoint requires:

- complete weight-first query semantics;
- working left-handed periodic input;
- exact half-open user wrapping distinct from backend primary remapping;
- exact reduced-basis minimum-image and duplicate-safety machinery;
- certified success/failure semantics for integer translations recovered from
  approximate native Cartesian positions;
- qualified poor but backend-resolvable equivalent lattices passing public
  forward smoke cases;
- no remaining ambiguity about public shift basis/sign semantics that would
  force WP5–WP9 to reinterpret earlier results.

### WP5 — Certify periodic 3D face-image shifts without finite search windows

**Purpose**

Replace 3D face-shift reconstruction whose correctness currently depends on a
finite coefficient search window.

**Main code areas**

- spatial face-shift reconstruction;
- shared boundary candidate enumeration;
- spatial public result packaging;
- normalization and tessellation diagnostics;
- spatial boundary tests.

**Required correctness model**

For each raw face in a periodic result that requires semantic owner/image
metadata:

1. preserve a trusted non-periodic wall record as a wall rather than sending it
   through generator-image enumeration; otherwise obtain the boundary geometry
   internally whether or not vertices were requested publicly;
2. certify whether the record represents positive boundary measure within the
   declared numerical envelope; certified-zero records are non-semantic raw
   artifacts, while unresolved near-zero records are ambiguous;
3. derive a finite Cartesian region that must contain every generator image
   compatible with a semantic positive-measure boundary;
4. enumerate the proof-complete reduced-basis integer candidates for that
   region;
5. evaluate candidates against the complete returned face geometry rather than
   a single seed point;
6. deduplicate only candidates defining the same exact cut and the same public
   semantic provenance, then require one remaining assignment;
7. map the shift to the user basis;
8. validate directed reciprocity where a reciprocal record exists.

Voro++ boundary vertices are approximate binary64 geometry. The v0.9 contract
therefore certifies candidate completeness relative to the returned geometry
and the declared internal numerical envelope; it must not claim exact knowledge
of an unobserved exact native face. In power mode, compatibility uses the exact
binary64 radii passed to Voro++ and an envelope that covers native radius
squaring/plane-offset arithmetic, while original weights remain the public
scientific representation. Ambiguity or inconsistency is a structured failure.
The implementation must not silently choose the candidate with the best residual
or mutate a result to manufacture reciprocity.

**Acceptance criteria**

- supported full/partial orthorhombic periodicity and full triclinic periodicity
  are covered, and real non-periodic walls in partial-periodic domains remain
  walls rather than generator-image candidates;
- standard and power diagrams are covered;
- persistent self-neighbor faces are covered and zero shift is never accepted
  as their periodic image;
- reciprocal directed faces carry opposite physical shifts;
- self-neighbor periodic records form `s`/`-s` pairs where reciprocal records
  exist;
- no user finite search window controls correctness;
- requesting face shifts works when public vertices/adjacency geometry is not
  requested; internally required geometry is removed before final packaging;
- normalization and diagnostics continue to interpret the new shifts and the
  positive-measure/non-semantic-raw boundary policy correctly;
- exactly coincident cuts with different owner/shift/kind provenance follow the
  documented ambiguity or canonical-provenance rule rather than residual
  ordering;
- standard and power fixtures include a case where public shifted weights and
  backend re-squared radii are detectably different;
- a small explicit-supercell/test oracle has independently justified coverage,
  and a short completeness/correctness note exists.

**Stop condition**

If a complete candidate set cannot be justified from returned geometry plus a
reasonable numerical envelope, stop and define the minimum native image-identity
contract needed. Do not fall back to a larger finite coefficient cube.

### WP6 — Certify periodic 2D edge owners and image shifts

**Purpose**

Replace finite planar edge reconstruction while preserving the additional 2D
responsibility of recovering hidden owner identity as well as the image shift.

**Main code areas**

- planar edge-shift/owner reconstruction;
- planar public result packaging;
- planar normalization and diagnostics;
- native-output characterization fixtures.

**Required behavior**

WP6 uses the same positive-measure, exact-provenance-equivalence, approximate
native-envelope, and backend-effective power-plane contract as WP5.

Negative native planar neighbor codes cannot by themselves decide whether a
boundary is a wall or a hidden periodic generator image. When identity is not
reliable, the certified candidate is the pair

```text
(owner generator, periodic shift)
```

rather than a shift for an already trusted owner ID.

**Acceptance criteria**

- full and partial rectangular periodicity are covered;
- standard and power diagrams are covered;
- hidden negative native adjacency is resolved without a finite search radius;
- periodic generator boundaries are not left classified as walls merely because
  of the native integer sign;
- self-neighbor edges and opposite shifts are handled;
- real walls remain distinguishable from periodic-image boundaries;
- reciprocity is validated without mutation;
- public edge shifts do not require public vertex output;
- empty/sparse fully periodic fixtures cover negative native codes and collapsed
  zero-length edge records; partial-periodic fixtures cover real walls and
  wall/generator coincident-plane ambiguity;
- bounded explicit-image/test oracles with independently justified coverage
  agree with production results;
- the current ineffective repair path is not preserved as meaningful public
  behavior.

WP6 may be developed in parallel with WP5 after the shared certification
contract is fixed, but the default single-maintainer order is WP5 then WP6 so
the simpler known-owner 3D case establishes the common completeness/error
model first.

### WP7 — Define ghost boundary identity and certify periodic ghost shifts

**Purpose**

Make periodic `ghost_cells()` boundary identity correct for persistent
neighbors, periodic images of the temporary ghost itself, and non-periodic
walls.

**Main code areas**

- 2D and 3D ghost bindings;
- ghost result normalization/packaging;
- shared boundary-reference schema;
- certified owner/image reconstruction;
- empty and sparse periodic regression fixtures.

**Required behavior**

Correctness authority is a joint semantic record consisting of boundary kind,
owner identity where applicable, and user-basis image shift. The shared
positive-measure and exact-provenance rules from WP5/WP6 apply. Native neighbor
integers can be used as hints or seeds but are not the public semantic source.

`p_ghost_site` is the active backend-primary/cell-site Cartesian representative
that anchors the returned ghost geometry. It is never the original unwrapped
query. Reanchoring before public packaging is allowed only if all shifts and
reconstruction equations are transformed consistently.

For a ghost self-image:

```text
p_image = p_ghost_site + shift @ A
shift != 0
```

For a persistent generator boundary:

```text
p_image = p_generator_original + shift @ A
```

For a wall, no periodic image shift is present.

Current-HEAD source inspection and focused execution establish that the 3D box
standard/power and periodic standard/power ghost routes can read an
uninitialized temporary ghost ID before Python normalization. WP7 therefore
requires a narrow native-bridge correction that prevents that read. Acceptable
designs include initializing one reserved internal temporary ID before native
neighbor extraction or using a ghost-specific binding path that does not request
indeterminate IDs. The internal seed is not the public semantic authority, and
an undocumented sentinel must not become public contract. A vendored Voro++
source patch triggers the D9 decision before acceptance.

**Acceptance criteria**

- empty persistent periodic container plus one ghost is covered where the
  backend supports the operation;
- one- and multiple-persistent-generator periodic systems are covered;
- standard and power modes are covered;
- 2D and 3D are covered;
- supported partial-periodic wall cases remain distinguishable from ghost
  self-images;
- ghost self boundaries have explicit `ghost_self` semantics and non-zero
  user-basis shifts;
- persistent-neighbor boundaries have stable generator IDs and user-basis
  shifts;
- no uninitialized 3D ghost neighbor value is read in any of the four
  standard/power box/periodic routes, rather than merely being replaced later;
- 2D native negative codes cannot silently reassign a ghost boundary to the
  wrong persistent generator;
- ghost boundary shifts do not force vertices into public output;
- weight-mode tests use WP1's whole-problem common gauge;
- repeated calls and repeated subprocesses prove deterministic initialized
  native behavior rather than accidental memory reuse;
- 2D empty/sparse cases cover native negative codes and non-semantic collapsed
  edges;
- internal schema validation rejects inconsistent kind/payload combinations.

The issue still characterizes all current-HEAD native routes before coding, but
the need for a 3D native-bridge correction is already decided. Pure geometric
reconstruction is not sufficient if the binding first reads indeterminate data.

### WP8 — Unify periodic query, owner, and ghost metadata

**Purpose**

Provide enough coherent user-lattice metadata that downstream code does not
need to reconstruct wrapping or owner image shifts manually, while keeping
internal geometry requirements independent of requested public output.

**Main code areas**

- central prepared-generator state;
- detached periodic geometry snapshots;
- spatial and planar `locate()`/`ghost_cells()` packaging;
- internal/public optional geometry handling.

**Required reconstruction semantics**

For a query:

```text
q_original = q_wrapped_user + q_shift @ A
```

For an owner image, the preferred stable reference is the original input
position of that persistent generator:

```text
p_owner_image = p_generator_original + owner_shift @ A
```

Ghost boundary shifts use the same user basis/sign convention and the
`p_ghost_site` definition fixed by WP0/WP7.

The result contract should distinguish conceptually among:

- original query coordinates;
- user-parallelepiped wrapped query coordinates;
- query integer shift;
- backend-used/cell-site coordinates where needed to interpret returned native
  geometry;
- owner generator ID;
- original owner generator coordinates;
- actual owner image coordinates;
- owner image shift;
- ghost boundary kind/ID/shift.

Exact field names are fixed in the target API inventory. A new large result
class hierarchy is not required if existing dictionaries or small immutable
views can express the contract cleanly.

**Internal state requirement**

Prepared generator state must stop using an ambiguous single notion of
"primary" when it needs several representatives. Internally distinguish, where
applicable:

- input Cartesian points;
- user-wrapped points and user wrap shifts;
- backend-primary points and backend remap shifts;
- native internal points.

**Acceptance criteria**

- every public periodic integer shift is in the user basis;
- 2D and 3D use the same conceptual sign equations;
- original, user-wrapped, and backend-used coordinates are never conflated;
- current `remap_cart()` meaning is unchanged;
- locate not-found rows have explicit masked/sentinel semantics;
- owner-image shifts recovered from approximate native Cartesian positions are
  returned only after WP4 certifies a unique compatible integer translation;
  ambiguity is structured rather than tie-broken by residual;
- external generator IDs change labels but not geometric shift calculation;
- empty ghost result sets still preserve query metadata;
- equivalent user bases may produce different wrapped representatives, but each
  result satisfies its own reconstruction equation and represents the same
  qualified physical image;
- internal vertices/adjacency needed for correctness are stripped when the
  corresponding public output was not requested.

### WP9 — Remove obsolete periodic reconstruction controls

**Purpose**

Remove public controls that cease to represent a meaningful scientific/user
choice after certified reconstruction.

**Required removal**

Remove the applicable spatial/planar forward and ghost parameters corresponding
to:

- finite face/edge shift search windows;
- user-disableable face/edge shift validation;
- mutation-based face/edge shift repair;
- public face/edge matching tolerances used only by reconstruction.

No warning-only compatibility period is required.

**Preserve separately**

- separator `image_search` as an ADR-0012 performance seed;
- general tessellation diagnostic tolerances;
- private structural proof/resource limits.

**Acceptance criteria**

- removed keywords are no longer accepted;
- no error message advises the user to enlarge a correctness search window;
- no public mutation-based repair remains in the certified boundary path;
- API inventory, migration text, and changelog match the final signatures;
- successful results no longer depend on a user-selected finite coefficient
  radius.

### Integration checkpoint B — Periodic topology and metadata

After WP5–WP9, perform a second independent adversarial periodic review.

The checkpoint requires:

- certified persistent 3D face and 2D edge owner/image shifts;
- self-neighbor coverage;
- correct positive-measure persistent/ghost-self/wall ghost identity, with
  certified-zero raw records excluded from semantic topology;
- no uninitialized 3D temporary ghost ID read on any supported route;
- exact provenance/canonicalization and structured ambiguity behavior;
- backend-effective power-plane certification;
- user-basis query/owner/ghost reconstruction equations and unique certified
  recovery from approximate native positions;
- internal required geometry separated from public requested geometry;
- obsolete correctness knobs removed;
- normalization and diagnostics still correct;
- no remaining internal consumer whose semantic decision relies only on the
  sign of a native neighbor integer.

After this checkpoint, the supported periodic contract should be treated as a
1.0 candidate and not casually reworked during the inverse packages.

### WP10 — Decouple separator observation, mismatch, hard-bound, and penalty spaces

**Purpose**

Allow scientifically natural mixed-space separator models such as optimizing
absolute separator displacement while constraining relative separator position,
without introducing the later generic mixed inverse composition system.

**Main code areas**

- separator constraints and model terms;
- problem compilation;
- objective/proximal/direct/ADMM paths;
- result views and reports;
- observation identity and numerical regression tests.

**Required public model**

Mismatch terms, hard constraints, and scalar boundary penalties can each select
`fraction` or `position` independently. An omitted term space inherits
`SeparatorObservations.measurement`.

The exact dataclass signatures are chosen so existing positional constructors do
not silently change meaning; adding `space` last or making it keyword-only is
preferred where needed.

`L2Regularization` remains a regularizer on weights and does not acquire a
separator measurement space.

**Compilation semantics**

Each term is transformed internally through the rowwise affine relation between
weight difference and the requested separator coordinate. Hard intervals become
weight-difference feasibility bounds. Penalty parameters remain expressed in
the term's declared measurement units; any row-dependent internal coefficients
that arise from coordinate conversion are mathematical consequences of the
transform, not a new public per-row policy feature.

**Result and identity semantics**

`SeparatorObservations.measurement` remains the observation source/target space
and remains part of observation/source identity. It is not silently redefined
as the objective space.

Observation-facing target/prediction/residual views should retain their source
meaning. The problem/result model additionally exposes explicit mismatch-space
semantics where necessary so users can tell which quantities the objective
actually used.

If the strict report exact-key contract must change, bump the report schema
version explicitly rather than silently widening schema v1. Observation row IDs
must not change merely because a model evaluates a different measurement space.

**Acceptance criteria**

- absolute-position mismatch with fractional hard bounds is a first-class
  supported case;
- mismatch, hard-bound, and scalar-penalty spaces can be selected independently;
- omitted spaces reproduce current v0.8 same-space behavior numerically;
- an independently manually transformed same-space problem agrees with the
  automatic mixed-space compiler;
- mixed-space infeasibility is detected in the compiled feasibility bounds;
- current direct/ADMM applicability rules remain unless the mathematics
  genuinely requires a change;
- reports/result views identify observation and mismatch/model spaces
  unambiguously;
- observation source identity/fingerprints remain stable;
- no arbitrary per-row spaces, robust scales, strengths, anchors, or generic
  observation-block API are added;
- no public point-centered convenience type is required for release.

### WP11 — Promote realization-aware separator fitting to a supported workflow

**Purpose**

Expose the normal realization-aware separator workflow through
`pyvoro2.inverse` without freezing the entire research/path control surface.

**Main code areas**

- high-level inverse exports/facade;
- the existing active-set entry point and result model;
- API inventory and lifecycle classification;
- public workflow tests.

**Required behavior**

- keep `fit_weights_from_separators(...)` as the fixed-observation mathematical
  solve;
- add a distinct supported high-level realization-aware function under
  `pyvoro2.inverse`;
- reuse the existing active engine and `SelfConsistentPowerFitResult` rather
  than redesigning final-state/provenance layers already established by ADRs
  0014–0015;
- expose the stable problem/model/inner-solver choices needed by normal callers
  plus a small outer lifecycle surface such as an iteration limit and requested
  final output layers;
- keep low-level initial active sets, hysteresis, relaxation, cycle-window,
  weight-step, and path/research controls in the advanced separator surface
  unless a concrete public need is established during issue review.

**Stable result semantics**

The supported result must preserve separate views of:

- the final inner fit;
- final realization, when available;
- candidate/final diagnostics;
- outer termination;
- availability of weights-dependent final layers.

Outer `cycle_detected`, `max_outer_iter`, active-set infeasibility, and numerical
failure are legitimate structured outcomes. A cycle or iteration limit may
still contain a coherent final state. Input/configuration errors remain
exceptions. Do not collapse these distinctions into one ambiguous `success`
boolean.

**Acceptance criteria**

- ordinary callers do not import an experimental namespace for the supported
  realization-aware workflow;
- fixed and realization-aware algorithms remain separate public functions;
- D6 mixed-space models work through the supported facade;
- structured outer non-success outcomes remain inspectable;
- unavailable weights-dependent layers remain `None` rather than fabricated
  placeholders;
- active path internals are not accidentally promoted to stable 1.0 surface.

### Integration checkpoint C — Inverse candidate API

After WP10–WP11, verify that:

- fixed same-space workflows have not regressed;
- at least one mixed-space problem is supported and independently checked;
- the realization-aware facade uses the same stabilized model semantics;
- observation/source and mismatch/model views are unambiguous;
- outer termination and final inner-fit status remain separate;
- ordinary supported use does not require `pyvoro2.inverse.separator`;
- advanced active controls remain explicitly advanced/provisional.

### WP12 — Qualify public forward, periodic, and separator workflows

**Purpose**

Turn the completed v0.9 semantics into executable downstream contracts using the
existing examples/integration structure rather than inventing a new
qualification framework.

**Main areas**

- `tests/integration/test_public_workflows.py` and focused public integration
  tests;
- existing forward/inverse regression suites;
- repository-owned preferred-API examples;
- structural benchmark/counter coverage where useful.

**Required workflow families**

- non-periodic weighted forward computation;
- orthorhombic full and partial periodicity;
- right-handed triclinic periodicity;
- valid left-handed lattice input;
- deliberately poor but backend-resolvable unimodularly equivalent lattices;
- stable generator IDs, measures, neighbors, and physical periodic shifts;
- weight-first `locate()`;
- weight-first `ghost_cells()`;
- persistent and ghost-self positive-measure boundary image metadata;
- empty/sparse ghost fixtures proving initialized native identity and documented
  degenerate-record behavior;
- consistent query/owner/ghost reconstruction equations, including exact user
  wrapping and certified approximate-native translation recovery;
- the supported realization-aware separator facade;
- at least one independent-measurement-space inverse model;
- structured outer non-success termination;
- normalization and diagnostics on the stabilized periodic results;
- repeated independent frames without requiring persistent native state.

A canonical downstream workflow must not require:

- `_internal` imports;
- manual weight-to-radius conversion;
- manual handedness repair;
- a home-grown minimum-image implementation;
- manual lattice-shift recovery from Cartesian positions;
- increasing a correctness-sensitive search radius;
- an experimental active-set namespace for ordinary realization-aware fitting.

**Extreme power-weight limitation**

Retain/add a platform-robust regression envelope for the known Voro++ numerical
resolution limitation. A qualified outcome may be a principled structured
wrapper/native-resolution failure, or finite geometry that passes the existing
consistency/diagnostic contract. Do not assert one compiler-specific corrupted
geometry or invent a universal numerical cutoff.

README capability/limitation text should remain explicit, analogous to other
unsupported/limited geometries such as oblique periodic 2D.

**Performance qualification**

Prefer structural metrics over fragile timing gates, including where
appropriate:

- proof-box/candidate counts before and after reduction;
- exact integer bit growth;
- reduction iteration/work counters;
- boundary/ghost candidate counts.

Absolute wall-clock benchmarks may remain informative but do not gate release.

**Acceptance criteria**

- all required workflow families pass using only public API;
- property/metamorphic tests use independent oracles rather than duplicating the
  production implementation;
- no qualification workflow relies on a compatibility surface scheduled for
  removal in WP9;
- all current supported output normalization/diagnostic consumers operate on
  the final result semantics.

### WP13 — Perform the final pre-1.0 API and lifecycle audit

**Purpose**

Review the implemented v0.9 surface only after qualification and answer for
each public semantic: "Do we want to promise this exact behavior after 1.0?"

**Audit scope**

- public namespaces and exports;
- function signatures/defaults;
- result schemas and metadata fields;
- exception versus structured-status boundaries;
- stable/advanced/provisional lifecycle classifications;
- residual compatibility fields or aliases;
- separator result/report convenience views;
- realization-aware advanced controls;
- 2D/3D asymmetries that no longer have a conceptual reason;
- stale search/tolerance/repair controls;
- current architecture/API-inventory/documentation consistency;
- D9 backend-fork policy before release.

**Required conclusions**

- `remap_cart()` still means backend/Voro++ primary remap, not user wrapping;
- every public periodic shift has the user-basis meaning from WP0, exact user
  wrapping remains distinct from approximate-native translation recovery, and
  neither uses an undocumented floating tie/repair heuristic;
- semantic boundary identity requires certified positive measure and exact
  provenance handling;
- ghost kind is not inferred from integer sign, and no uninitialized 3D temporary
  ghost ID is read;
- power-boundary certification uses backend-effective radii while public power
  semantics remain mathematical weights;
- `ghost_radius` is gone;
- obsolete face/edge correctness knobs are gone;
- separator observation and mismatch/model spaces are not ambiguous;
- stable realization-aware semantics are promoted, while research/path controls
  remain advanced;
- no already-known incompatible v0.9.x change remains intentionally deferred in
  the forward/periodic/separator core.

Only narrow current-API/reference/migration/changelog/limitation maintenance
belongs here. Documentation information-architecture and visual-stack redesign
remain a separate post-functional planning pass.

### WP14 — Qualify the frozen v0.9.0 source and artifacts

**Purpose**

Qualify the exact release candidate after the API audit rather than accepting a
moving source tree based only on intermediate issue checks.

**Required validation**

- full repository test suite;
- supported Python/platform CI matrix;
- wheel builds and installed-wheel smoke tests;
- sdist-to-wheel round trip and distribution-content checks;
- public workflow tests against installed artifacts where the existing release
  tooling supports them;
- strict documentation build for the factual/API/capability documentation updated
  for v0.9.0;
- API inventory/changelog/version metadata consistency;
- platform-tolerant extreme-power limitation regression;
- milestone/release-plan outcome review.

Any tracked correction after freezing the candidate invalidates the qualified
source hash and requires renewed source/artifact qualification, following the
existing release workflow.

## Dependency and implementation order

The intended dependency graph is:

```text
WP0  Release and semantic contracts
|
|-- WP1  Weight-first query parity
|
|-- WP2  User lattice + orientation-neutral backend frame
|    |
|    `-- WP3  Exact unimodular reduction
|         |
|         `-- WP4  Reduced certified geometry + native translation recovery
|              |
|              |-- WP5  Certified 3D face shifts
|              `-- WP6  Certified 2D owner/edge shifts
|                   |
|          WP1 + WP5 + WP6
|                   |
|                   `-- WP7  Ghost identity and shifts
|                        |
|              WP2 + WP4 + WP7
|                        |
|                        `-- WP8  Periodic metadata/internal-output separation
|                             |
|                   WP5 + WP6 + WP7 + WP8
|                             |
|                             `-- WP9  Periodic API lifecycle cleanup
|
`-- WP10  Separator measurement-space redesign
     |
     `-- WP11  Supported realization-aware facade

WP1-WP11
    |
    `-- WP12  Public workflow qualification
         |
         `-- WP13  Final API/lifecycle audit
              |
              `-- WP14  Frozen-source release qualification
```

### Default implementation phases

**Phase A — periodic foundations:** WP0–WP4, followed by checkpoint A.

**Phase B — periodic topology and metadata:** WP5–WP9, followed by checkpoint B.

**Phase C — inverse stabilization:** WP10–WP11, followed by checkpoint C.

**Phase D — qualification and freeze:** WP12–WP14.

WP1, WP2, WP3 preparation, and WP10 can proceed independently after the
relevant WP0 contracts are fixed. WP2 introduces or contracts the foundational
exact wrap primitives; WP3 extends them with reduction. WP5/WP6 can technically
proceed in parallel after their shared certification model exists, but the
default single-maintainer order is WP5 then WP6. WP7 semantic reconstruction
waits for both persistent-boundary paths, while its narrow 3D native-bridge child
task may proceed after WP0/WP1 and current-route characterization. WP9 waits
until certified replacements exist. WP13 is deliberately last among API
decisions so it audits observed behavior rather than freezing assumptions.

## Issue-scoped implementation and review workflow

The repository's normal issue-scoped agent handoff remains authoritative. For
v0.9, high-risk numerical work adds a stronger review loop rather than a
separate development system.

### 1. Prepare each issue from the current `dev` head

Before implementation, a reasoning/review pass reads:

- this active plan;
- the linked issue;
- relevant ADRs and API inventory;
- the actual current source after already accepted work packages.

It re-evaluates the issue-local implementation architecture against current
code, fixes the acceptance contract and independent test oracle, records any
stop condition, and then prepares the coding-agent prompt. Later implementation
prompts are intentionally not generated in advance because preceding accepted
changes may make a cleaner implementation possible.

### 2. One substantial issue per coding-agent chain

The coding agent implements only the linked issue, including production code,
tests, narrow current documentation/API-inventory/changelog updates, and
reported validation. It must not invent a new public policy or broaden the plan
non-goals.

### 3. Independent adversarial review

A fresh reasoning/review pass reads the complete diff, runs the relevant tests,
uses independent mathematical/property oracles where required, checks hidden API
changes and scope creep, and tries to falsify the claimed invariants.

The review ends with either an explicit acceptance or a finite list of blocking
problems. The next dependent work package should not be built on an unaccepted
foundation.

### 4. Minimal correction loop

When review rejects the implementation, the coding agent receives a correction
prompt limited to the blocking findings. A fresh review follows the correction.

### 5. Maintainer acceptance

Only after independent acceptance does the maintainer treat the work package as
complete and integrate/close it under the normal `dev`/issue workflow.

### Contract-first/high-risk packages

The exact-wrap portion of WP2, certified native-translation portion of WP4,
WP3, WP5, WP6, WP7, and the result/report semantics portion of WP10 require a
contract/test-oracle pass before ordinary production coding begins.

A reasoning model may implement a small correctness-critical kernel, independent
test oracle, pathological fixture generator, or narrow schema normalizer when
the contract cannot be cleanly separated from a small implementation. If it
does, acceptance must still come from a separate fresh review rather than the
same reasoning pass that authored the code.

Routine API plumbing, validation, signatures, packaging, ordinary facade code,
and broad regression-test wiring remain coding-agent work.

## Compatibility and migration

v0.9 intentionally uses the remaining pre-1.0 freedom to remove known awkward
surfaces rather than preserving them indefinitely.

### Intentional immediate breaks

| Existing behavior/surface | v0.9 policy |
|---|---|
| `ghost_radius` | Remove; use `ghost_radii` for explicit radii and `ghost_weights` for mathematical weights. |
| Face/edge finite shift-search controls | Remove after certified reconstruction. |
| Face/edge shift validation/repair controls | Remove from the public correctness path. |
| Public reconstruction-only face/edge matching tolerances | Remove or make private implementation constants. |
| Ghost neighbor identity overloaded into native integer sign/value | Prevent indeterminate 3D ID reads and replace the ghost semantic contract with explicit boundary kind plus validated payload. |
| Degenerate raw native faces/edges treated as semantic topology | Assign semantic boundary identity only after positive measure is certified; record raw retention/normalization behavior explicitly. |
| Exact-tie user-coefficient ordering | Replace with physical-displacement tie selection; only exact-degenerate image identity may change. |
| Heuristic `PeriodicCell` rejection of poor but exact non-degenerate bases | Permit mathematical construction; report actual backend/resource failure at the operation that encounters it. |
| Strict report exact-key schema if WP10 requires new space fields | Bump schema version explicitly rather than changing v1 silently. |

No deprecation-only compatibility aliases are required for the first four
categories.

### Additive/preferred changes

- weight-first `locate()`;
- weight-first `ghost_cells()`;
- valid left-handed `PeriodicCell` input;
- user fractional-coordinate and user-parallelepiped wrapping helpers;
- coherent query/owner/ghost periodic metadata;
- model-term `space=None` inheritance;
- a supported realization-aware separator facade.

### Semantics that must not change accidentally

- radii remain a backend representation and weights remain the mathematical
  power representation;
- one common additive weight gauge remains global for one complete power
  problem;
- `remap_cart()` retains its Voro++-primary meaning;
- an explicitly supplied separator image shift still identifies that image and
  is not replaced by a nearer image;
- separator `image_search` remains correctness-neutral;
- observation/source identity is not changed by model-term measurement spaces;
- fixed inner fitting remains distinct from empirical outer refinement;
- outer realization-aware termination is not replaced by the final inner-fit
  status;
- public shifts are never returned in the private reduced basis;
- exact user wrapping decisions are not replaced by floating inverse/floor
  heuristics;
- original weights remain the public power semantics even when native-boundary
  certification uses backend-effective radii;
- one repository/distribution is retained through 1.0.

## Validation strategy

### Unit tests

Use focused unit tests for:

- exact determinant/dyadic lattice representation;
- exact `U`/`U_inverse` and shift round trips;
- reduction determinism and structural resource limits;
- sign-normalized backend frame construction;
- left-handed improper orthogonal transforms;
- Cartesian/fractional conversion and exact half-open wrapping decisions at
  seams, exact interior remainders whose binary64 view reaches an upper
  endpoint, one ULP to either side, negative coordinates, large shears, and
  left-handed bases;
- unique/ambiguous/inconsistent integer-translation recovery from approximate
  native Cartesian positions;
- whole-problem ghost weight gauge conversion;
- positive-measure boundary eligibility and boundary-reference kind/payload
  validation;
- backend-effective standard/power plane compatibility and exact provenance
  equivalence;
- initialized 3D native ghost IDs across all standard/power box/periodic routes;
- term-specific separator-space affine compilation;
- report/result space semantics;
- supported realization-aware facade argument normalization.

### Representation metamorphic tests

For a benign row basis `A` and bounded exact integer unimodular matrix `U`, use

```text
A_prime = U @ A
```

and verify that equality in exact dyadic arithmetic before comparing physical
invariants. A rounded matrix product that represents a perturbed lattice is not
a basis-invariance fixture. Corresponding shifts must satisfy

```text
s = s_prime @ U
```

rather than raw shift tuples being equal.

Compare as applicable:

- physical minimum-image displacement and exact distance;
- duplicate classification;
- generator identity;
- cell measures;
- neighbor topology for fixtures with a certified positive margin from
  degeneracy;
- documented ambiguity/normalization behavior rather than raw backend record
  counts for exact or near-degenerate fixtures;
- physical face/edge translations;
- reconstructed owner/query image coordinates.

Include determinant `+1` and `-1` transforms. Different equivalent bases can
produce different wrapped representatives because they define different
fundamental parallelepipeds; tests require each representative to satisfy its
own reconstruction equation rather than requiring raw wrapped coordinates to be
identical.

### Exact-tie tests

Construct deliberate half-cell/symmetry ties and verify:

- the selected physical image is invariant under equivalent user/reduced bases;
- pair reversal negates the selected displacement/shift;
- translating an endpoint by a lattice vector transforms the integer shift
  covariantly;
- exact reduction strategy does not alter the selected physical tie;
- orientation `+1`/`-1` applies the documented smallest/largest exact Cartesian
  tuple rule in fixed caller axes.

### Independent boundary-image oracles

Production reconstruction must not validate itself. Use several independent
checks:

1. ordinary/radical bisector equations over all returned boundary vertices;
2. directed reciprocity for persistent cells;
3. explicit small-supercell non-periodic references for selected fixtures;
4. bounded brute-force image enumeration as a test-only oracle;
5. unimodular representation metamorphisms;
6. explicit ghost-insertion references for sparse fixtures;
7. Cartesian reconstruction from user-basis shifts.

A small supercell or bounded coefficient cube is an oracle only when its
coverage is justified independently, for example by a geometric exclusion bound
or a proven stabilization margin. It must not repeat the same finite-window
assumption production is removing.

Ghost-self boundaries normally lack a persistent reciprocal record, so their
oracles emphasize geometry, explicit insertion, and owner-kind/image
reconstruction rather than reciprocity alone. Repeated-process 3D tests cover all
four standard/power box/periodic routes; 2D tests include empty/sparse periodic
negative codes and collapsed edges. Exact coincident cuts with different
provenance and power cases where ideal weights differ detectably from
re-squared backend radii must be included.

### Inverse numerical/property tests

Required independent checks include:

- same-space v0.8 regressions;
- manual relative/absolute separator transformation;
- automatic mixed-space compilation versus an independently manually
  transformed same-space model;
- mixed-space hard infeasibility;
- direct/ADMM agreement where both paths are mathematically applicable;
- unchanged observation/source identity under model-space changes;
- structured active termination and final-layer availability cases.

### Integration qualification

Public integration tests use only public API and cover all workflow families in
WP12. They verify user-visible invariants rather than private algorithm choices.

### Performance/regression boundary

Release gates prefer structural workload counters to absolute timing:

- proof candidate/box size;
- exact candidate count;
- reduction iteration/work count;
- integer bit growth;
- boundary/ghost candidate count.

Private resource limits can yield deterministic success or a structured
resource failure, never an uncertified approximate fallback. Timing benchmarks
may track trends but do not become a fragile release gate. Within qualified
numerical ranges, batched and separate ghost-weight calls should also agree; an
extreme-range backend limitation must become an inspectable failure or narrowly
documented capability boundary rather than unexplained geometry drift.

## Documentation outputs

Functional implementation precedes the separate documentation-overhaul pass.
Within this plan, update only documentation required to keep repository
contracts accurate:

- this release plan and its outcome/revisions;
- periodic and inverse decision records created/amended by WP0;
- API inventory/lifecycle classifications;
- exact public API reference for changed names, defaults, result fields, and
  statuses;
- narrow migration/changelog entries for intentional breaks;
- architecture text where current responsibilities change;
- preferred public examples/integration workflow documentation;
- README capability/limitation wording for the known extreme power-weight
  resolution limitation if implementation/audit shows it needs alignment.

These factual/API/capability updates are the only documentation gate for
v0.9.0. Content/information-architecture improvement and then visual/navigation
improvement or documentation-engine migration are separate, explicitly planned
v0.9.x soak-period work. They do not delay v0.9.0 and do not have to finish
before the soak begins.

## Non-goals

v0.9.0 does not include:

- prescribed cell-area or cell-volume inverse fitting;
- mixed separator-plus-measure inversion;
- a generic per-row/group inverse composition API;
- arbitrary user-selected per-row hard-bound policies, robust-loss scales,
  penalty strengths, or measurement spaces;
- per-site inverse anchors/regularization;
- exact clipped non-periodic diagrams influenced by generators outside the
  requested `Box`;
- oblique periodicity in 2D;
- partial triclinic periodicity in 3D;
- public arbitrary wall/convex domains;
- automatic global numerical similarity scaling;
- solving the upstream Voro++ extreme power-weight dynamic-range limitation;
- a public Niggli/Minkowski/lattice-reduction API;
- automatic Voro++ execution on a reduced equivalent basis;
- regular-triangulation/dual public APIs;
- persistent native containers or trajectory/MD APIs;
- GIL/parallel-performance work without profiling evidence;
- broad typing, cosmetic, or large-file refactors unrelated to a required
  semantic change;
- deep refactoring solely because an existing function is large;
- a forward/inverse package split;
- documentation-stack redesign.

For non-periodic `Box` domains, supporting generators outside the requested box
is a future geometry capability. Correct support requires separating
computational/storage domain from requested clipping domain and defining new
clipping/output semantics; it is not a v0.9 stabilization fix.

## Risks and mitigations

| Risk | Consequence | Mitigation |
|---|---|---|
| Exact reduction is mathematically correct but gives little practical proof-work improvement. | Representation robustness remains impractical for the cases that motivate WP3. | Compare algorithms against adversarial candidate/work metrics before production commitment; stop rather than hide another finite radius. |
| Exact integer reduction causes pathological coefficient/bit growth. | Resource blow-up or platform-sensitive behavior. | Track exact structural counters, use Python integers internally, define private deterministic work limits, and fail structurally rather than approximately. |
| Improper backend transform reverses orientation-sensitive geometry. | Vertex winding or signed-normal regressions despite correct distances/measures. | Explicitly audit winding/normals in WP2 and add left-handed metamorphic fixtures. |
| Boundary completeness is asserted from one vertex or a heuristic envelope. | Face/edge shifts can still be wrong outside tests. | Contract-first WP5/WP6, all-boundary-geometry validation, independent supercell/brute-force oracles, and a documented completeness argument. |
| Rounded inverse/floor controls a half-open wrap under a poor valid basis. | A public integer shift can be wrong while the float point looks plausible. | Decide discrete user wrapping from exact dyadic arithmetic; treat floats only as validated rounded views. |
| Approximate native image coordinates are inverted without certification. | A wrong owner-image translation can be selected near a lattice boundary. | Require one uniquely compatible integer translation within the declared envelope; otherwise fail structurally. |
| Zero-measure native records receive semantic identity. | Numerical polygon artifacts become unstable topology. | Certify positive measure before tagging; make certified-zero records non-semantic and unresolved records ambiguous. |
| Ideal input weights are used as the native power-plane oracle. | Certification can disagree with the radii/arithmetic Voro++ actually consumed. | Use exact passed binary64 radii plus an envelope covering native squaring/offset arithmetic. |
| Native ghost neighbor IDs are treated as reliable when they are not. | Ghost self-images can be mislabeled as generators/walls or expose indeterminate IDs. | Require a 3D native-bridge correction that prevents the read, then apply tagged semantic reconstruction; settle D9 before any vendored source patch. |
| Ghost schema redesign spills into unnecessary ordinary-result churn. | Large avoidable breaking surface. | Require semantic normalization internally; change ordinary `compute()` result fields only where correctness or coherent public contract demands it. |
| Mixed separator spaces accidentally change observation identity or old result semantics. | Stored/reproducibility contracts break for reasons unrelated to the scientific model. | Keep observation measurement/source identity fixed, add explicit mismatch/model views, and version strict reports if their schema changes. |
| Realization-aware promotion freezes research/path internals. | 1.0 carries knobs that are difficult to support scientifically. | Keep the high-level facade small; leave hysteresis/path controls in the advanced namespace unless an issue proves a stable downstream need. |
| Poor but valid lattice is accepted by the constructor but Voro++ cannot resolve it. | Users may interpret later failure as a regression. | Separate mathematical validity, backend representability, and certified-resource failure in diagnostics and qualification wording. |
| Extreme power-weight native behavior varies by compiler/platform. | Brittle regression tests or false promise of a universal safety threshold. | Test a behavior envelope: structured failure or finite/consistent geometry; do not assert corrupted geometry or a guessed cutoff. |
| v0.9 becomes a general cleanup/refactor release. | Scope and review become unbounded before 1.0. | Enforce issue-local non-goals, checkpoints, and final API audit; defer cosmetic and unrelated architecture work. |

## Risk and implementation-mode classification

| Work package | Risk | Default implementation mode | Required review emphasis |
|---|---|---|---|
| WP0 | Architectural | Maintainer + reasoning/review model | Repository contract and activation approval |
| WP1 | Low/medium | Coding agent | Gauge/input parity and compatibility |
| WP2 | Medium/high | Coding agent | Exact wrapping, orientation, user/backend semantics, numerical failure lifecycle |
| WP3 | Very high | Contract-first; reasoning-assisted + coding agent | Exact mappings, termination/resource proof, independent oracle |
| WP4 | High | Coding agent after oracle | Basis invariance, duplicate safety, exact ties, native-translation certification |
| WP5 | Very high | Contract-first coding agent | Candidate completeness, floating/native envelope, reciprocity |
| WP6 | Very high | Contract-first coding agent | Joint owner+shift reconstruction and wall distinction |
| WP7 | Very high | Contract-first; reasoning-assisted + coding agent | Native characterization, ghost-self schema, Python/C++ boundary |
| WP8 | High | Coding agent | Cross-dimensional metadata equations and output stripping |
| WP9 | Low | Coding agent | Clean removal and inventory/migration consistency |
| WP10 | High | Contract-first for result/report semantics; coding agent | Affine equivalence, identity/provenance, schema versioning |
| WP11 | Medium | Coding agent | Stable facade versus advanced active controls |
| WP12 | High | Coding agent + independent review | Public-only workflows and independent oracles |
| WP13 | High | Reasoning/review led | 1.0 promise surface and residual compatibility |
| WP14 | Release-critical | Maintainer/CI/release tooling | Exact frozen source and artifact qualification |

## Work-package issue decomposition

Issue #47 is the substantive execution tracker. The following are intended
just-in-time child-issue titles, not an instruction to pre-create the full issue
set:

1. **WP0 — [#46 — Activate the v0.9.0 functional/API stabilization plan](https://github.com/DeloneCommons/pyvoro2/issues/46)**
2. **WP1 — `[v0.9] Add weight-first locate and ghost workflows`**
3. **WP2 — `[v0.9] Separate user lattice semantics from the backend periodic frame`**
4. **WP3 — `[v0.9] Add exact private lattice-basis reduction and shift mappings`**
5. **WP4 — `[v0.9] Use reduced bases for certified periodic geometry and native translation recovery`**
6. **WP5 — `[v0.9] Certify periodic 3D face-image shifts without finite search windows`**
7. **WP6 — `[v0.9] Certify periodic 2D edge owners and image shifts`**
8. **WP7 — `[v0.9] Define ghost boundary identity and certify periodic ghost-image shifts`**
9. **WP8 — `[v0.9] Unify periodic query, owner, and ghost metadata in the user lattice basis`**
10. **WP9 — `[v0.9] Remove obsolete periodic shift reconstruction controls`**
11. **WP10 — `[v0.9] Decouple separator observation, mismatch, hard-bound, and penalty spaces`**
12. **WP11 — `[v0.9] Promote realization-aware separator fitting to a supported workflow`**
13. **WP12 — `[v0.9] Qualify the public forward, periodic, and separator workflows`**
14. **WP13 — `[v0.9] Perform the final pre-1.0 API and lifecycle audit`**
15. **WP14 — `[v0.9] Qualify the frozen v0.9.0 source and artifacts`**

These are release-structure units, not a promise that no focused child issue can
be created if implementation uncovers an independently reviewable defect. Such
a finding should be split and linked rather than silently expanding an existing
issue.

Each child issue body should be prepared from the then-current `dev` state and
describe observable outcomes, accepted decisions, tests/docs, compatibility
impact, and stop conditions. It should not duplicate this entire plan or freeze
an implementation recipe that may become stale after earlier packages land.

## Public API inventory

WP0 updates the existing [API inventory](../api-inventory.md) from the v0.8
baseline into a target/implementation ledger for v0.9 public changes. At minimum
it must track:

- `PeriodicCell` exact half-open user fractional/wrapping operations and the
  unchanged backend-oriented remap operations;
- left-handed-input validity semantics;
- `locate()` mathematical-weight input;
- `ghost_weights`/`ghost_radii` and removal of `ghost_radius`;
- periodic boundary-reference/ghost metadata schema, positive-measure
  eligibility, and raw degenerate-record retention/normalization behavior;
- original/user-wrapped/backend-site/native coordinate vocabulary and
  query/owner/ghost shift fields;
- structured ambiguity for coincident provenance and approximate-native
  translation recovery;
- backend-effective power-plane certification semantics;
- removed face/edge search/validation/repair/matching controls;
- separator model-term measurement spaces and result/report views;
- the supported realization-aware high-level function;
- stable versus advanced/provisional active-set controls and result semantics.

The inventory is updated as work is accepted and is approved during WP13 rather
than reconstructed after implementation.

## Release acceptance criteria

All of the following are required before v0.9.0 enters the downstream soak
period:

1. `compute()`, `locate()`, and `ghost_cells()` have coherent mathematical
   weight/explicit-radius semantics, including one whole-problem ghost weight
   gauge.
2. Valid left- and right-handed periodic bases are accepted without permuting
   user lattice vectors or changing user integer coefficient meaning.
3. User fractional/user-parallelepiped operations are distinct from
   Voro++-primary remapping, and the integer half-open wrap shift is decided by
   exact dyadic arithmetic rather than floating inverse/floor heuristics.
4. Certified nearest/minimum-image and mandatory periodic duplicate-safety
   machinery remain practical for the qualified family of poor equivalent
   bases, subject only to explicit structural resource limits.
5. Exact nearest-image ties use D1's complete exact Cartesian total order and
   all returned public shifts are mapped to the user basis.
6. Integer owner/image translations recovered from approximate native Cartesian
   positions are returned only after unique compatibility is certified within
   the declared envelope; ambiguity or inconsistency is structured.
7. Supported 3D face and 2D edge image shifts no longer depend on a user finite
   search window and cover standard/power, supported partial/full periodicity,
   and self-neighbor cases.
8. Semantic boundary identity is assigned only to certified-positive-measure
   faces/edges; certified-zero raw records and unresolved near-zero records
   follow the documented non-semantic/ambiguity policy.
9. Exact coincident candidates are deduplicated only when both their exact cut
   and public semantic provenance agree; different owner/shift/kind provenance
   is not selected by residual ordering.
10. Power-boundary certification uses the exact binary64 radii supplied to
    Voro++ plus a documented envelope for native squaring/offset arithmetic,
    while mathematical weights remain the public scientific representation.
11. Periodic ghost boundaries reliably distinguish persistent generators,
    ghost self-images, and walls, including sparse/empty periodic fixtures.
12. No uninitialized 3D temporary ghost ID is read on any supported
    standard/power box/periodic route; internal seed values are not public
    semantic authority.
13. Query, owner, and ghost metadata obey documented reconstruction equations,
    define `p_ghost_site` as the active cell-site representative, and do not
    conflate original input, user wrapping, backend-primary, or native internal
    coordinates.
14. Vertices/adjacency needed internally for certified reconstruction are not
    forced into public results when the user did not request them.
15. Obsolete public face/edge search, validation, repair, and
    reconstruction-only tolerance controls are removed, and `ghost_radius` is
    removed without a deprecated alias.
16. Separator mismatch, hard constraints, and scalar penalties can use
    independent supported measurement spaces while omitted term spaces preserve
    v0.8 same-space behavior.
17. Observation/source identity remains stable and reports/results describe
    observation versus mismatch/model spaces unambiguously.
18. A supported realization-aware separator function exists under
    `pyvoro2.inverse` without requiring ordinary callers to use the advanced
    separator namespace.
19. Outer active-set non-success outcomes remain structured and distinct from
    final inner-fit status; coherent final states are not discarded merely
    because the outer loop cycles or reaches its iteration limit.
20. Canonical public workflows require no private imports, manual
    weight-to-radius conversion, manual handedness repair, custom minimum-image
    code, manual shift recovery, correctness-window tuning, or experimental
    active namespace.
21. Representation, exact-tie, boundary-image, ghost, and mixed-space tests use
    independently justified metamorphic or explicit oracles rather than only
    production self-comparison or another unexplained finite search window.
22. The extreme power-weight/Voro++ resolution limitation has a platform-robust
    regression envelope and accurate narrow capability wording, without a
    fabricated universal cutoff.
23. The final API inventory, lifecycle classifications, ADRs, current reference,
    migration notes, and changelog match implemented behavior; the complete
    repository/release validation and supported wheel/artifact matrix pass on
    the exact frozen release source.
24. No known critical correctness or semantic hole remains in the
    forward/periodic/separator-inverse core that already requires a planned
    incompatible v0.9.x change.

Meeting these gates starts the v0.9.x downstream-readiness/soak period. It does
not claim that external downstream qualification has already occurred; that
real-world evidence is the purpose of the released v0.9.x series before 1.0.

## Completion matrix

This matrix describes evidence required from each package; issue/milestone state
remains the detailed progress tracker.

| Work package | Production change | Unit/property tests | Independent oracle/correctness note | Public workflow evidence | API/docs/changelog |
|---|---|---|---|---|---|
| WP0 | Repository contracts | — | Architecture review | — | Required |
| WP1 | Required | Required | Gauge metamorphism | Qualified in WP12 | Required |
| WP2 | Required | Required | Exact wrap + transform/reconstruction equations | Qualified in WP12 | Required |
| WP3 | Required | Required | Mandatory | Indirect | ADR/correctness note |
| WP4 | Required | Required | Brute-force + metamorphic + native-translation certification | Qualified in WP12 | Required |
| WP5 | Required | Required | Mandatory | Qualified in WP12 | Required |
| WP6 | Required | Required | Mandatory | Qualified in WP12 | Required |
| WP7 | Required, including native bridge | Required | Mandatory | Qualified in WP12 | Required |
| WP8 | Required | Required | Reconstruction equations | Qualified in WP12 | Required |
| WP9 | Removal | Signature/regression | — | Migration smoke | Required |
| WP10 | Required | Required | Manual transform | Qualified in WP12 | ADR/report update |
| WP11 | Required | Required | Existing-engine comparison | Qualified in WP12 | Required |
| WP12 | Small/adaptive | Required | Cross-workflow | Central purpose | Examples/current docs |
| WP13 | Cleanup only | Regression | Audit evidence | Re-run | Central purpose |
| WP14 | No feature work | Full suite | Artifact/source qualification | Full | Release records |

## Linked decisions and issues

Current decision records that directly constrain this plan:

- [ADR 0002 — Weights, radii, gauge, and component offsets](../decisions/0002-weights-radii-and-gauge.md)
- [ADR 0012 — Certified periodic nearest-image geometry](../decisions/0012-certified-periodic-image-geometry.md)
- [ADR 0013 — Central generator preparation and backend safety](../decisions/0013-central-generator-preparation-and-backend-safety.md)
- [ADR 0014 — Separator observation and source identity](../decisions/0014-separator-observation-and-source-identity.md)
- [ADR 0015 — Atomic separator active-set final state](../decisions/0015-atomic-separator-active-state.md)
- [ADR 0016 — Severity-complete tessellation diagnostics](../decisions/0016-severity-complete-tessellation-diagnostics.md)
- [ADR 0017 — Functional stabilization precedes 1.0 and later inverse families](../decisions/0017-v0.9-functional-stabilization-before-1.0.md)
- [ADR 0018 — User-lattice periodic semantics and certified boundary identity](../decisions/0018-periodic-user-lattice-and-boundary-semantics.md)
- [ADR 0019 — Independent separator measurement spaces and supported realization-aware fitting](../decisions/0019-separator-measurement-spaces-and-supported-realization.md)

Execution links:

- [`v0.9.0` milestone](https://github.com/DeloneCommons/pyvoro2/milestone/3)
- [#46 — Activate the v0.9.0 functional/API stabilization plan](https://github.com/DeloneCommons/pyvoro2/issues/46)
- [#47 — Complete v0.9.0 functional and API stabilization](https://github.com/DeloneCommons/pyvoro2/issues/47)

Detailed implementation evidence and child-issue progress belong in GitHub;
this plan remains the source-controlled implementation authority.

## Plan revisions

| Date | Revision | Change | Reason |
|---|---|---|---|
| 2026-09-01 | 0.4 | Activated the reviewed plan under milestone `v0.9.0`; linked #46/#47; recorded ADR 0012's physical-space tie amendment, ADRs 0018–0019, and the pre-implementation v0.9 target API/lifecycle ledger. | Complete WP0 so WP1–WP11 can proceed without unresolved contract ambiguity while retaining D9 as its explicit later gate. |
| 2026-08-31 | 0.3 | Completed a fresh post-edit blueprint review; clarified binary64 views at exact wrap boundaries, exact construction of basis-invariance fixtures, and non-periodic wall handling in 3D periodic results. | Prevent test or implementation details from weakening the corrected semantic contracts. |
| 2026-08-31 | 0.2 | Incorporated the final independent current-HEAD review: corrected backend-frame algebra; completed tie, exact wrapping, boundary, ghost-native, power-plane, provenance, testing, lifecycle, and documentation contracts; settled one repository/distribution through 1.0. | Remove known blueprint-level correctness and authority inconsistencies before activation. |
| 2026-08-31 | 0.1 | Initial repository draft from the code-first v0.9 architectural review and maintainer decisions D1–D8. | Establish the implementation blueprint before milestone/issue activation. |

## Outcome

Complete this section during release review before archiving the plan:

- release and date;
- delivered outcomes and any explicitly omitted conditional work;
- compatibility/breaking-change result;
- validation and artifact-qualification summary;
- downstream-soak handoff;
- deferred work and follow-up issues;
- links to the milestone, release notes, changelog, final API inventory, and
  relevant decision records.
