name: Wheels

on:
  push:
    tags:
      - "v*"
  pull_request:
    branches: [dev, main]
  workflow_dispatch:

env:
  CIBW_SKIP: >-
    cp3??t-* pp* gp* *-musllinux* *-manylinux_i686
    *-manylinux_{aarch64,ppc64le,s390x,armv7l,riscv64}
    *-win32 *-win_arm64 *-macosx_universal2
  CIBW_TEST_EXTRAS: "test"
  # These distributed compiler/target cohorts have no ordinary planar
  # qualification. Require explicit refusal and record each exact native
  # profile/module hash in the wheel test log. Qualified Ubuntu/GCC 13.3 source
  # wheels instead pass the default success gate in CI's build-dist job.
  CIBW_TEST_COMMAND: >-
    python -m pip check &&
    python "{project}/tools/check_installed_package.py"
    --repo-root "{project}" --require-scipy --planar-refusal

jobs:
  wheels-manylinux-x86_64:
    name: Wheels (manylinux x86_64, planar refusal)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Build and test wheels
        uses: pypa/cibuildwheel@v3.4.1
        env:
          CIBW_ARCHS_LINUX: "x86_64"
          CIBW_BUILD: >-
            cp310-manylinux_x86_64 cp311-manylinux_x86_64
            cp312-manylinux_x86_64 cp313-manylinux_x86_64
            cp314-manylinux_x86_64

      - name: Upload wheels
        uses: actions/upload-artifact@v6
        with:
          name: wheels-manylinux-x86_64
          path: wheelhouse/*.whl
          if-no-files-found: error

  wheels-windows-amd64:
    name: Wheels (Windows AMD64, planar refusal)
    runs-on: windows-latest
    steps:
      - name: Preserve reviewed source bytes on checkout
        run: git config --global core.autocrlf false
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Build and test wheels
        uses: pypa/cibuildwheel@v3.4.1
        env:
          CIBW_ARCHS_WINDOWS: "AMD64"
          CIBW_BUILD: >-
            cp310-win_amd64 cp311-win_amd64 cp312-win_amd64
            cp313-win_amd64 cp314-win_amd64

      - name: Upload wheels
        uses: actions/upload-artifact@v6
        with:
          name: wheels-windows-amd64
          path: wheelhouse/*.whl
          if-no-files-found: error

  wheels-macos-arm64:
    name: Wheels (macOS arm64, planar refusal)
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Build and test wheels
        uses: pypa/cibuildwheel@v3.4.1
        env:
          CIBW_ARCHS_MACOS: "arm64"
          CIBW_BUILD: >-
            cp310-macosx_arm64 cp311-macosx_arm64 cp312-macosx_arm64
            cp313-macosx_arm64 cp314-macosx_arm64

      - name: Upload wheels
        uses: actions/upload-artifact@v6
        with:
          name: wheels-macos-arm64
          path: wheelhouse/*.whl
          if-no-files-found: error

  wheels-macos-x86_64:
    name: Wheels (macOS x86_64, planar refusal)
    runs-on: macos-15-intel
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Build and test wheels
        uses: pypa/cibuildwheel@v3.4.1
        env:
          CIBW_ARCHS_MACOS: "x86_64"
          CIBW_BUILD: >-
            cp310-macosx_x86_64 cp311-macosx_x86_64
            cp312-macosx_x86_64 cp313-macosx_x86_64
            cp314-macosx_x86_64

      - name: Upload wheels
        uses: actions/upload-artifact@v6
        with:
          name: wheels-macos-x86_64
          path: wheelhouse/*.whl
          if-no-files-found: error

  sdist:
    name: Build sdist
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Build sdist
        run: |
          python -m pip install --upgrade pip build
          python -m build --sdist

      - name: Upload sdist
        uses: actions/upload-artifact@v6
        with:
          name: sdist
          path: dist/*.tar.gz
          if-no-files-found: error

  validate:
    name: Validate release artifacts
    needs:
      - wheels-manylinux-x86_64
      - wheels-windows-amd64
      - wheels-macos-arm64
      - wheels-macos-x86_64
      - sdist
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.14"

      - name: Download complete artifact set
        uses: actions/download-artifact@v7
        with:
          pattern: "*"
          path: release-dist
          merge-multiple: true

      - name: Install artifact validators
        run: |
          python -m pip install --upgrade pip
          python -m pip install twine

      - name: Validate wheel matrix and distribution contents
        run: |
          python tools/check_wheel_matrix.py release-dist
          python tools/check_dist.py release-dist
          python tools/check_dist_metadata.py release-dist

      - name: Create clean aggregate-wheel environment
        run: python -m venv "${{ runner.temp }}/pyvoro2-wheel-venv"

      - name: Check aggregate manylinux wheel and explicit planar refusal
        shell: bash
        run: |
          "${{ runner.temp }}/pyvoro2-wheel-venv/bin/python" \
            -m pip install --upgrade pip
          "${{ runner.temp }}/pyvoro2-wheel-venv/bin/python" \
            -m pip install \
            release-dist/pyvoro2-*-cp314-cp314-manylinux*x86_64.whl
          "${{ runner.temp }}/pyvoro2-wheel-venv/bin/python" -m pip check
          "${{ runner.temp }}/pyvoro2-wheel-venv/bin/python" \
            tools/check_installed_package.py \
            --repo-root "${{ github.workspace }}" --forbid-scipy --planar-refusal \
            | tee aggregate-wheel-profile.txt

      - name: Preserve aggregate wheel profile and refusal evidence
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: aggregate-wheel-planar-refusal-profile
          path: aggregate-wheel-profile.txt

      - name: Upload release-ready distributions
        uses: actions/upload-artifact@v6
        with:
          name: release-distributions
          path: |
            release-dist/*.whl
            release-dist/*.tar.gz
          if-no-files-found: error
